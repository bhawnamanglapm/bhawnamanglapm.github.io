{
  "name": "bhawnamangla-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for the portfolio site. The site itself is static HTML/CSS/JS served by GitHub Pages; this only bundles the Amplitude analytics entry point into a committed browser script.",
  "scripts": {
    "build": "esbuild src/analytics.js --bundle --format=iife --minify --target=es2018 --outfile=amplitude-init.js"
  },
  "dependencies": {
    "@amplitude/unified": "^1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
